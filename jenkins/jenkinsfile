pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'python -m py_compile server/main.py'
                stash(name: 'compiled-results', includes: 'sources/*.py*') 
            }
        }
        stage('Deliver') {
            agent {
                docker {
                    image 'cdrx/pyinstaller-linux:python2'
                }
            }
            steps {
                sh 'pyinstaller --onefile server/main.py'
            }
            post {
                success {
                    archiveArtifacts 'dist/main'
                }
            }
        }
    }
}
